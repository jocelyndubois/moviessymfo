<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8"/>
    <title>{% block title %}MoviesSymfo!{% endblock %}</title>
    {% block stylesheets %}
        {% stylesheets 'bundles/movies/css/*' filter='cssrewrite' %}
        <link rel="stylesheet" href="{{ asset_url }}"/>
        {% endstylesheets %}
    {% endblock %}
    <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}"/>
</head>
<body>
<div class="site-wrapper">

    <div class="site-wrapper-inner">

        <div class="cover-container">

            <div class="masthead clearfix">
                <div class="inner">
                    <h3 class="masthead-brand">MoviesSymfo</h3>
                    <nav>
                        <ul class="nav masthead-nav">
                            <form class="navbar-form navbar-left" role="search" action="{{ path('_searchmovie') }}"
                                  method="get">
                                <div class="input-group">
                                    <input type="text" class="form-control" name="movie" placeholder="Search for...">
                          <span class="input-group-btn">
                            <button class="btn btn-default" type="submit"><span class="glyphicon glyphicon-search"
                                                                                aria-hidden="true"></span></button>
                          </span>
                                </div>
                                <!-- /input-group -->
                            </form>
                        </ul>
                    </nav>
                </div>
            </div>

            <div class="inner cover">
                {% block body %}
                {% endblock %}
                {% block javascripts %}
                    {% javascripts '@MoviesBundle/Resources/public/js/*' %}
                    <script type="text/javascript" src="{{ asset_url }}"></script>
                    {% endjavascripts %}
                {% endblock %}
            </div>

        </div>

    </div>

</body>
</html>
